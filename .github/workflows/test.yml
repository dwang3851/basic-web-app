name: basic-web-app

on:

push:

branches:

- main

jobs:

  lint-and-test:

    runs-on: ubuntu-latest

    name: CI workflow

    steps:

    - uses: actions/checkout@v3

    - uses: bahmutov/npm-install@v1

    - run: npm run lint

    - run: npm run test